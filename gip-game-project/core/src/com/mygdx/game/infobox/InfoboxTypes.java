package com.mygdx.game.infobox;

public interface InfoboxTypes {
    enum CardInfoType {
        ENCODE,
        COMPILE,
        EXHAUST,
        DEXTERITY,
        DISCARD_PILE
    }
    enum ItemInfoType {
        DATADISK
    }
}
